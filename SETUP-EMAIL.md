# Инструкция по настройке отправки Email

## 📋 Что было добавлено:

### 1. **HTML форма** (`index.html`)

- ✅ Поле для загрузки файлов (множественный выбор)
- ✅ Поддержка всех форматов документов: PDF, DOC, DOCX, XLS, XLSX, XML, TXT, изображения, архивы
- ✅ Поле необязательное для заполнения

### 2. **CSS стили** (`css/modal.css`)

- ✅ Стильная область для загрузки файлов
- ✅ Отображение списка выбранных файлов с размером
- ✅ Кнопки удаления файлов из списка

### 3. **JavaScript** (`script.js`)

- ✅ Управление загрузкой файлов
- ✅ Отображение выбранных файлов
- ✅ Отправка формы с файлами на сервер

### 4. **PHP обработчик** (`send-email.php`)

- ✅ Обработка формы
- ✅ Отправка email с вложениями
- ✅ Красивое HTML письмо
- ✅ Поддержка как стандартной функции mail(), так и SMTP

---

## 🔧 Настройка на хостинге

### Вариант 1: Использование стандартной функции mail() PHP

Это самый простой способ, если ваш хостинг поддерживает функцию `mail()`.

**Что нужно сделать:**

1. **Откройте файл `send-email.php`** (строки 6-8)

2. **Замените данные на свои:**

```php
$emailTo = 'daniilbortvin@gmail.com';  // Email получателя
$emailFrom = 'noreply@china-components.ru';  // Email отправителя (ВАЖНО: должен быть на вашем домене!)
$emailFromName = 'China Components Website';  // Имя отправителя
```

3. **Загрузите файл `send-email.php`** в корневую папку сайта на хостинге

4. **Готово!** Проверьте работу формы

**Важно:** Email отправителя (`$emailFrom`) должен быть на вашем домене, например:

- ✅ `noreply@china-components.ru`
- ✅ `info@china-components.ru`
- ✅ `orders@china-components.ru`
- ❌ НЕ `gmail.com`, `mail.ru` и т.д.

---

### Вариант 2: Использование SMTP (более надежно)

Если стандартная функция `mail()` не работает или письма попадают в спам, используйте SMTP.

**Что нужно от хостинг-провайдера:**

1. **SMTP сервер** - например: `smtp.your-hosting.com`
2. **Порт** - обычно `587` (TLS) или `465` (SSL)
3. **Логин** - обычно ваш email: `noreply@china-components.ru`
4. **Пароль** - пароль от этого email
5. **Тип шифрования** - `TLS` или `SSL`

**Настройка SMTP в `send-email.php`:**

1. **Раскомментируйте строки 10-16** (уберите `/*` и `*/`):

```php
$smtpHost = 'smtp.your-host.com';  // SMTP сервер от хостинга
$smtpPort = 587;  // Порт (587 для TLS, 465 для SSL)
$smtpUsername = 'noreply@china-components.ru';  // Email
$smtpPassword = 'ваш_пароль';  // Пароль от email
$smtpSecure = 'tls';  // 'tls' или 'ssl'
```

2. **Установите PHPMailer** (если его нет на сервере):

   - Скачайте PHPMailer: https://github.com/PHPMailer/PHPMailer
   - Создайте папку `phpmailer` на сервере
   - Загрузите в неё файлы: `PHPMailer.php`, `SMTP.php`, `Exception.php`

3. **Раскомментируйте блок кода SMTP** в `send-email.php` (строки 123-163):

   Уберите `/*` перед строкой 123 и `*/` после строки 163

---

## 📧 Где взять данные SMTP?

### Популярные хостинги:

**Beget:**

- SMTP: `smtp.beget.com`
- Port: `465` (SSL) или `2525`
- Логин/пароль: данные от почтового ящика

**Timeweb:**

- SMTP: `smtp.timeweb.ru`
- Port: `25` или `587`
- Логин/пароль: данные от почтового ящика

**Reg.ru:**

- SMTP: `smtp.reg.ru`
- Port: `587` (TLS)
- Логин/пароль: данные от почтового ящика

**SpaceWeb:**

- SMTP: `smtp.spaceweb.ru`
- Port: `587`
- Логин/пароль: данные от почтового ящика

**Обычно данные указаны в панели управления хостингом в разделе "Почта" или "Email".**

---

## ✅ Проверка работы

1. Загрузите `send-email.php` на хостинг
2. Откройте сайт
3. Нажмите на любую кнопку "ОСТАВИТЬ ЗАЯВКУ"
4. Заполните форму
5. Попробуйте прикрепить файлы (необязательно)
6. Нажмите "ОТПРАВИТЬ"
7. Проверьте email: `daniilbortvin@gmail.com`

---

## 🐛 Возможные проблемы:

### Письма не приходят:

- Проверьте папку "Спам"
- Убедитесь что `$emailFrom` на вашем домене
- Попробуйте SMTP вместо `mail()`

### Ошибка при отправке:

- Проверьте права на файл `send-email.php` (должны быть 644 или 755)
- Проверьте лог ошибок PHP на хостинге
- Убедитесь что хостинг поддерживает отправку email

### Файлы не прикрепляются:

- Проверьте лимиты размера файлов в `php.ini`:
  - `upload_max_filesize` (рекомендуется минимум 10M)
  - `post_max_size` (рекомендуется минимум 20M)
- Обратитесь к хостинг-провайдеру для увеличения лимитов

---

## 📞 Техподдержка

Если возникли проблемы:

1. Проверьте лог ошибок PHP на хостинге
2. Откройте консоль браузера (F12) и проверьте ошибки JavaScript
3. Свяжитесь с техподдержкой хостинга для настройки SMTP

---

## 🎯 Что сейчас работает:

- ✅ Форма с полями: Имя, Телефон
- ✅ Загрузка файлов (необязательно, множественная)
- ✅ Валидация полей
- ✅ Красивые модальные окна
- ✅ Отображение списка выбранных файлов
- ✅ PHP обработчик для отправки email
- ✅ Поддержка вложений любого формата
- ✅ Адаптивный дизайн

**Осталось только:**

1. Загрузить `send-email.php` на хостинг
2. Настроить email в файле (строки 6-8 или 10-16 для SMTP)
3. Проверить работу!
